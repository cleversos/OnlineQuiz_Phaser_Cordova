const userData = require('./users');
const roomData = require('./rooms');
const wordData = require('./words');
const infoData = require('./infos');

module.exports = {
  users: userData,
  rooms: roomData,
  words: wordData,
  infos: infoData,
};
